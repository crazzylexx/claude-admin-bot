# 🤖 Claude Admin Bot

**Telegram-бот для удаленного управления и общения с Claude Code через Telegram и веб-интерфейс.**

![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)
![aiogram](https://img.shields.io/badge/aiogram-3.13.1-green.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.109.0-teal.svg)

---

## 📋 Описание

Claude Admin Bot — это мощный инструмент для удаленного взаимодействия с Claude Code. Проект включает два компонента:

1. **Telegram Bot** (`bot.py`) — полноценный бот с поддержкой:
   - 💬 Текстовых сообщений (передаются напрямую в Claude)
   - 📤 Загрузки файлов на сервер
   - 📥 Скачивания файлов с сервера
   - ⏳ Статусов обработки (для долгих запросов)
   - 🔒 Защиты по Admin ID

2. **Web Interface** (`api.py` + `webapp/index.html`) — киберпанк-стиль веб-интерфейс с:
   - 🌐 WebSocket соединением в реальном времени
   - 🎨 Красивым UI в стиле терминала
   - 📁 Поддержкой загрузки файлов и изображений
   - 💻 Блоками кода с кнопкой копирования

---

## 🚀 Быстрый старт

### Требования

- **Python 3.11+**
- **Telegram Bot Token** (получить у [@BotFather](https://t.me/BotFather))
- **Claude CLI** (установлен и настроен на сервере)

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/crazzylexx/claude-admin-bot.git
   cd claude-admin-bot
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   
   # Windows
   venv\Scripts\activate
   
   # Linux/Mac
   source venv/bin/activate
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте `.env` файл:**
   ```env
   # Telegram Bot Token
   CLAUDE_BOT_TOKEN=your_bot_token_here
   
   # Ваш Telegram ID (узнать у @userinfobot)
   ADMIN_TELEGRAM_ID=your_telegram_id
   ```

5. **Создайте директорию для файлов:**
   ```bash
   mkdir files
   ```

---

## 🎮 Использование

### Запуск Telegram-бота

```bash
python bot.py
```

**Команды:**
- `/start` — приветственное сообщение и инструкции
- Любой текст — передается в Claude Code
- Отправка файла — сохраняется в `files/`
- Отправка пути (например, `/root/file.txt`) — бот отправит файл вам

### Запуск веб-интерфейса

```bash
python api.py
```

Откройте браузер: `http://localhost:8005`

**Возможности:**
- 💬 Чат с Claude в реальном времени
- 📤 Загрузка файлов через интерфейс
- 📷 Загрузка изображений
- 💻 Копирование блоков кода одним кликом

---

## 📁 Структура проекта

```
claude-admin-bot/
├── bot.py                 # Telegram-бот (polling)
├── api.py                 # FastAPI WebSocket API
├── webapp/
│   └── index.html         # Веб-интерфейс (киберпанк-стиль)
├── files/                 # Загружаемые файлы (создается автоматически)
├── requirements.txt       # Python зависимости
├── .env                   # Конфигурация (не в Git)
├── .gitignore            # Игнорируемые файлы
└── README.md             # Документация
```

---

## 🔧 Конфигурация

### Переменные окружения (`.env`)

| Переменная | Описание | Пример |
|------------|----------|--------|
| `CLAUDE_BOT_TOKEN` | Токен Telegram-бота | `123456:ABC-DEF...` |
| `ADMIN_TELEGRAM_ID` | ID администратора | `372886754` |

### Настройки в коде

**`bot.py` и `api.py`:**
- `WORK_DIR` — рабочая директория для Claude (по умолчанию `/root`)
- `FILES_DIR` — директория для загружаемых файлов
- Таймауты обработки: 10 сек (быстрый ответ), до 5 минут (максимум)

---

## 🛡️ Безопасность

- ✅ Доступ только для указанного `ADMIN_TELEGRAM_ID`
- ✅ `.env` файл исключен из Git
- ✅ Все процессы Claude изолированы (не мешают основной сессии)
- ⚠️ **Важно:** Не публикуйте токен бота в открытом доступе!

---

## 🧪 Тестирование

```bash
# Запуск тестов (будет добавлено)
pytest tests/
```

---

## 🐛 Известные проблемы

- **Windows:** Код изначально написан для Linux. Требуется адаптация:
  - Замена `preexec_fn=os.setsid` на Windows-совместимый код
  - Изменение путей `/root` на кроссплатформенные
- **Claude CLI:** Должен быть установлен и доступен в PATH

---

## 📝 Roadmap

- [ ] Адаптация для Windows
- [ ] Добавление unit-тестов
- [ ] Поддержка нескольких администраторов
- [ ] История сообщений (база данных)
- [ ] Docker-контейнер для развертывания
- [ ] CI/CD pipeline

---

## 🤝 Вклад в проект

Приветствуются Pull Request'ы! Для больших изменений:

1. Создайте Issue для обсуждения
2. Создайте ветку: `git checkout -b feature/amazing-feature`
3. Зафиксируйте изменения: `git commit -m 'feat: add amazing feature'`
4. Отправьте в GitHub: `git push origin feature/amazing-feature`
5. Создайте Pull Request

---

## 📄 Лицензия

MIT License — свободно используйте и модифицируйте.

---

## 👨‍💻 Автор

**crazzylexx**
- GitHub: [@crazzylexx](https://github.com/crazzylexx)

---

## 🙏 Благодарности

- [aiogram](https://github.com/aiogram/aiogram) — отличный фреймворк для Telegram-ботов
- [FastAPI](https://fastapi.tiangolo.com/) — быстрый и современный веб-фреймворк
- [Claude](https://www.anthropic.com/) — мощный AI-ассистент

---

**Сделано с ❤️ и ☕**
